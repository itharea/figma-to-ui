# Dependencies
node_modules/

# Lockfiles — single dependency; let each package manager generate its own
bun.lock
bun.lockb
package-lock.json
pnpm-lock.yaml
yarn.lock

# macOS
.DS_Store

# Work artifacts (intermediates produced while decoding a .fig)
message.json
*.message.json
ex/
out.svg
out.png
out.html

# Compiled Design IR output (build-ir.mts --out ir-<name>)
ir-*/

# Source design files (large binaries — keep out of git)
*.fig

# Logs / editor
*.log
.vscode/
.idea/
